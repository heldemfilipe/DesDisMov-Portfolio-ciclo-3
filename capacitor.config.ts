import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PortfolioCiclo3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
